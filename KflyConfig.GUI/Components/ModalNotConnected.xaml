<UserControl x:Class="KFly.GUI.ModalNotConnected"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Background="{DynamicResource AccentColorBrush}" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Label Foreground="White" Margin="10,20,10,10" FontSize="14"
               Content="You must be connected to the KFly controller to use this tab"></Label>
        <Button Name="openConnectionSettings" Margin="10,0,30,20" Content="Open connection settings"
                 Style="{StaticResource AccentedSquareButtonStyle}" Padding="7"
                                Click="openConnectionSettings_Click" HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>
